{
  "version": 2,
  "builds": [
    {
      "src": "src/backend/server.js",
      "use": "@vercel/node"
    },
    {
      "src": "src/frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/backend/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "src/frontend/dist/$1"
    }
  ],
  "installCommand": "npm install --prefix src/backend && npm install --prefix src/frontend --legacy-peer-deps",
  "buildCommand": "npm run build --prefix src/frontend"
}
