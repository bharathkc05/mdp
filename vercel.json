{
  "buildCommand": "cd src/frontend && npm install --legacy-peer-deps --include=dev && npm run build",
  "outputDirectory": "src/frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
